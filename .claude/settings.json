{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"PRE-COMMIT CHECKLIST (CLAUDE.md Pre-commit rules):\\n1. Check the repo for irrelevant files — update .gitignore if any should be excluded\\n2. Check if CLAUDE.md needs updating to reflect the changes being committed\\nComplete both checks before proceeding with the commit.\"}}'",
            "statusMessage": "Checking pre-commit rules..."
          }
        ]
      }
    ]
  }
}
